# Development dependencies. Installs runtime deps too.
#   pip install -r requirements-dev.txt   (or: uv pip install -r requirements-dev.txt)
-r requirements.txt

ruff==0.8.6
pytest==8.3.4
pre-commit==4.0.1
